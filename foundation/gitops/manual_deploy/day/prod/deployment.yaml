apiVersion: apps/v1
kind: Deployment
metadata:
  name: day
  namespace: day-ns
  labels:
    app: day
    tier: prod
    cluster: trantor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: day
  template:
    metadata:
      labels:
        app: day
        tier: prod
        cluster: trantor
    spec:
      containers:
      - name: day
        image: day:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
          name: http
        envFrom:
        - configMapRef:
            name: day-config
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
